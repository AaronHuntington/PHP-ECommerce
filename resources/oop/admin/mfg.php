<?php

class mfg_admin{


    // mkdir(PUBLIC_DIRECTORY.'testssss');
    // fopen(PUBLIC_DIRECTORY.'TEstic.php','hellio');

    // $myfile = fopen(PUBLIC_DIRECTORY."newfile.php", "w") or die("Unable to open file!");
    // $txt = "John Doe\n";
    // $myfile_a = fopen(PUBLIC_DIRECTORY."newfile.php", "wr") or die("Unable to open file!");

    // fwrite(PUBLIC_DIRECTORY.$myfile_a, $txt);

    // $txt = "user id sss";
    // $myfile = file_put_contents(PUBLIC_DIRECTORY.'newfile.php', $txt.PHP_EOL , FILE_APPEND | LOCK_EX);

    // $txt = "Jane Doe\n";
    // fwrite(PUBLIC_DIRECTORY.$myfile, $txt);
    // fclose(PUBLIC_DIRECTORY.$myfile);






}//class mfg_admin

?>